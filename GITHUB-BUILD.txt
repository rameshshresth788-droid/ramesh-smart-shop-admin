RAMESH SMART SHOP ADMIN - GitHub Actions build

The workflow uses the Android SDK already installed on GitHub-hosted runners.
It no longer runs android-actions/setup-android, which was the failing step in
Build Debug APK #1. It uses JDK 17, installs Android 36/build-tools 36.0.0,
uses the Gradle wrapper, builds assembleDebug, and uploads the APK artifact.

Workflow: .github/workflows/build-apk.yml
